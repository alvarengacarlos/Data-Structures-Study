struct list* buildList();
int push(int, struct list*);
int unshift(int, struct list*);
int shift(struct list*);
int pop(struct list*);
int showList(struct list*);
int searchElementByPosition(int, struct list*);
int searchElement(int, struct list*);
int destroyList(struct list*);